import { Rubik } from "next/font/google";
import { Prociono } from "next/font/google";

export const rubik = Rubik({ subsets: ["latin"] });
export const prociono = Prociono({ weight: "400", subsets: ["latin"] });
